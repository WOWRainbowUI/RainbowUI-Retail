-- Portuguese (Brazil) localization for BetterBlizzFrames
-- Localização em português (Brasil) para BetterBlizzFrames

local locale = GetLocale()
if locale ~= "ptBR" then return end

local L = BBF.L

-- Falls back to English (enUS) if translation is not provided
-- Any key needing update in translation will either be commented out completely or tagged --needsUpdate
-- Retorna ao inglês (enUS) se a tradução não for fornecida

