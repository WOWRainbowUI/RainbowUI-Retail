-- Italian localization for BetterBlizzFrames
-- Localizzazione italiana per BetterBlizzFrames

local locale = GetLocale()
if locale ~= "itIT" then return end

local L = BBF.L

-- Falls back to English (enUS) if translation is not provided
-- Any key needing update in translation will either be commented out completely or tagged --needsUpdate
-- Torna all'inglese (enUS) se la traduzione non è fornita

