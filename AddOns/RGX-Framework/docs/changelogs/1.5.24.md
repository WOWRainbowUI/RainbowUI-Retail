# v1.5.24 - 2025-04-29

## Fixes

- **Dropdowns**: Fixed UIDropDownMenu schema compatibility in NormalizeItems to properly convert legacy menu formats (menuList→children, arg1→value) and preserve legacy callbacks (func). This fixes font dropdowns rendering blank when passed legacy-style item tables.

## Technical Details

The RGXDropdowns module now transparently handles both native RGXDropdowns item schema and legacy UIDropDownMenu-compatible schemas within the same NormalizeItems function, ensuring backward compatibility while maintaining the modern RGXDropdowns API.
