## v2.0.0-alpha.2

- fix: SharedMedia startup scan scheduling
  - coalesce light and full rescans instead of running the expensive generic pass immediately
  - defer the generic addon-global scan until after `PLAYER_LOGIN`
  - allow late media-provider loads to upgrade a pending scan without duplicating work
- goal: reduce `[RGX:timer-slow] SharedMedia:QueueScan ...` startup warnings during normal login
