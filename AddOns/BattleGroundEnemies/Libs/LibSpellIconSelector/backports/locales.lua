if LOCALE_deDE then
	if not ICON_SELECTION_CLICK then ICON_SELECTION_CLICK = "Klicken, um in der Liste zu sehen" end
	if not ICON_SELECTION_DRAG then ICON_SELECTION_DRAG = "Symbol hierher ziehen, um es als Euer Anzeigebild zu verwenden" end
	if not ICON_SELECTION_NOTINLIST then ICON_SELECTION_NOTINLIST = "Dieses Symbol ist nicht in der Liste" end
	if not ICON_SELECTION_TITLE_CURRENT then ICON_SELECTION_TITLE_CURRENT = "Aktuell ausgewählt" end
	if not ICON_SELECTION_TITLE_CUSTOM then ICON_SELECTION_TITLE_CUSTOM = "Benutzerdefiniertes Symbol" end
	if not MACRO_POPUP_CHOOSE_ICON then MACRO_POPUP_CHOOSE_ICON = "Ein Symbol wählen:" end
elseif LOCALE_esES then
	if not ICON_SELECTION_CLICK then ICON_SELECTION_CLICK = "Haz clic para verlo en la lista" end
	if not ICON_SELECTION_DRAG then ICON_SELECTION_DRAG = "Arrastra un icono aquí para usarlo como representación" end
	if not ICON_SELECTION_NOTINLIST then ICON_SELECTION_NOTINLIST = "Este icono no está en la lista" end
	if not ICON_SELECTION_TITLE_CURRENT then ICON_SELECTION_TITLE_CURRENT = "Seleccionado actualmente" end
	if not ICON_SELECTION_TITLE_CUSTOM then ICON_SELECTION_TITLE_CUSTOM = "Icono personalizado" end
	if not MACRO_POPUP_CHOOSE_ICON then MACRO_POPUP_CHOOSE_ICON = "Selecciona un icono:" end
elseif LOCALE_esMX then
	if not ICON_SELECTION_CLICK then ICON_SELECTION_CLICK = "Haz clic para ver en la lista" end
	if not ICON_SELECTION_DRAG then ICON_SELECTION_DRAG = "Arrastra una imagen aquí para usarla como ícono" end
	if not ICON_SELECTION_NOTINLIST then ICON_SELECTION_NOTINLIST = "Este ícono no está en la lista" end
	if not ICON_SELECTION_TITLE_CURRENT then ICON_SELECTION_TITLE_CURRENT = "Selección actual" end
	if not ICON_SELECTION_TITLE_CUSTOM then ICON_SELECTION_TITLE_CUSTOM = "Ícono personalizado" end
	if not MACRO_POPUP_CHOOSE_ICON then MACRO_POPUP_CHOOSE_ICON = "Selecciona un ícono:" end
elseif LOCALE_frFR then
	if not ICON_SELECTION_CLICK then ICON_SELECTION_CLICK = "Cliquer pour afficher dans la liste" end
	if not ICON_SELECTION_DRAG then ICON_SELECTION_DRAG = "Déplacer une icône ici pour l’utiliser à l’écran" end
	if not ICON_SELECTION_NOTINLIST then ICON_SELECTION_NOTINLIST = "Cette icône n’est pas dans la liste" end
	if not ICON_SELECTION_TITLE_CURRENT then ICON_SELECTION_TITLE_CURRENT = "Sélection actuelle" end
	if not ICON_SELECTION_TITLE_CUSTOM then ICON_SELECTION_TITLE_CUSTOM = "Icône personnalisée" end
	if not MACRO_POPUP_CHOOSE_ICON then MACRO_POPUP_CHOOSE_ICON = "Sélectionnez une icône:" end
elseif LOCALE_itIT then
	if not ICON_SELECTION_CLICK then ICON_SELECTION_CLICK = "Clicca per consultare l'elenco" end
	if not ICON_SELECTION_DRAG then ICON_SELECTION_DRAG = "Trascina un'icona qui per usarla come immagine" end
	if not ICON_SELECTION_NOTINLIST then ICON_SELECTION_NOTINLIST = "Quest'icona non è nella lista" end
	if not ICON_SELECTION_TITLE_CURRENT then ICON_SELECTION_TITLE_CURRENT = "Attualmente selezionata" end
	if not ICON_SELECTION_TITLE_CUSTOM then ICON_SELECTION_TITLE_CUSTOM = "Icona personalizzata" end
	if not MACRO_POPUP_CHOOSE_ICON then MACRO_POPUP_CHOOSE_ICON = "Scegli un'icona:" end
elseif LOCALE_koKR then
	if not ICON_SELECTION_CLICK then ICON_SELECTION_CLICK = "목록을 보려면 클릭" end
	if not ICON_SELECTION_DRAG then ICON_SELECTION_DRAG = "디스플레이로 사용할 아이콘을 이곳으로 끌어다 놓기" end
	if not ICON_SELECTION_NOTINLIST then ICON_SELECTION_NOTINLIST = "목록에 없는 아이콘입니다." end
	if not ICON_SELECTION_TITLE_CURRENT then ICON_SELECTION_TITLE_CURRENT = "현재 선택됨" end
	if not ICON_SELECTION_TITLE_CUSTOM then ICON_SELECTION_TITLE_CUSTOM = "사용자 설정 아이콘" end
	if not MACRO_POPUP_CHOOSE_ICON then MACRO_POPUP_CHOOSE_ICON = "아이콘 선택:" end
elseif LOCALE_ptBR or LOCALE_ptPT then
	if not ICON_SELECTION_CLICK then ICON_SELECTION_CLICK = "Clique para vê-la na lista" end
	if not ICON_SELECTION_DRAG then ICON_SELECTION_DRAG = "Arraste um ícone para cá para usá-lo como sua tela" end
	if not ICON_SELECTION_NOTINLIST then ICON_SELECTION_NOTINLIST = "Este ícone não está na lista" end
	if not ICON_SELECTION_TITLE_CURRENT then ICON_SELECTION_TITLE_CURRENT = "Seleção atual" end
	if not ICON_SELECTION_TITLE_CUSTOM then ICON_SELECTION_TITLE_CUSTOM = "Ícone personalizado" end
	if not MACRO_POPUP_CHOOSE_ICON then MACRO_POPUP_CHOOSE_ICON = "Selecione um Ícone:" end
elseif LOCALE_ruRU then
	if not ICON_SELECTION_CLICK then ICON_SELECTION_CLICK = "Щелкните, чтобы просмотреть в виде списка" end
	if not ICON_SELECTION_DRAG then ICON_SELECTION_DRAG = "Перетащите сюда значок, который хотите использовать" end
	if not ICON_SELECTION_NOTINLIST then ICON_SELECTION_NOTINLIST = "Этого значка нет в списке" end
	if not ICON_SELECTION_TITLE_CURRENT then ICON_SELECTION_TITLE_CURRENT = "Текущий выбор" end
	if not ICON_SELECTION_TITLE_CUSTOM then ICON_SELECTION_TITLE_CUSTOM = "Пользовательский значок" end
	if not MACRO_POPUP_CHOOSE_ICON then MACRO_POPUP_CHOOSE_ICON = "Выберите значок:" end
elseif LOCALE_zhCN then
	if not ICON_SELECTION_CLICK then ICON_SELECTION_CLICK = "点击在列表中浏览" end
	if not ICON_SELECTION_DRAG then ICON_SELECTION_DRAG = "将一个图标拖曳至此处来显示" end
	if not ICON_SELECTION_NOTINLIST then ICON_SELECTION_NOTINLIST = "此图标不在列表中" end
	if not ICON_SELECTION_TITLE_CURRENT then ICON_SELECTION_TITLE_CURRENT = "当前已选择" end
	if not ICON_SELECTION_TITLE_CUSTOM then ICON_SELECTION_TITLE_CUSTOM = "自定图标" end
	if not MACRO_POPUP_CHOOSE_ICON then MACRO_POPUP_CHOOSE_ICON = "选择一个图标：" end
elseif LOCALE_zhTW then
	if not ICON_SELECTION_CLICK then ICON_SELECTION_CLICK = "點擊以在列表中查看" end
	if not ICON_SELECTION_DRAG then ICON_SELECTION_DRAG = "拖曳一個圖示至此處以做為你的顯示" end
	if not ICON_SELECTION_NOTINLIST then ICON_SELECTION_NOTINLIST = "此圖示不在列表裡" end
	if not ICON_SELECTION_TITLE_CURRENT then ICON_SELECTION_TITLE_CURRENT = "目前選擇" end
	if not ICON_SELECTION_TITLE_CUSTOM then ICON_SELECTION_TITLE_CUSTOM = "自訂圖示" end
	if not MACRO_POPUP_CHOOSE_ICON then MACRO_POPUP_CHOOSE_ICON = "選擇一個圖示：" end
else
	if not ICON_SELECTION_CLICK then ICON_SELECTION_CLICK = "Click to view in the list" end
	if not ICON_SELECTION_DRAG then ICON_SELECTION_DRAG = "Drag an icon here to use it as your display" end
	if not ICON_SELECTION_NOTINLIST then ICON_SELECTION_NOTINLIST = "This icon is not in the list" end
	if not ICON_SELECTION_TITLE_CURRENT then ICON_SELECTION_TITLE_CURRENT = "Currently Selected" end
	if not ICON_SELECTION_TITLE_CUSTOM then ICON_SELECTION_TITLE_CUSTOM = "Custom Icon" end
	if not MACRO_POPUP_CHOOSE_ICON then MACRO_POPUP_CHOOSE_ICON = "Choose an Icon:" end
end